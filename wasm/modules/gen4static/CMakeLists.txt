if(EMSCRIPTEN)
    add_executable(gen4static bridge/gen4static_bridge.cpp)
    target_include_directories(gen4static PRIVATE bridge "${POKEFINDER_ROOT}")
    target_compile_options(gen4static PRIVATE "-O3")
    target_link_options(gen4static PRIVATE "-O3" "-sMODULARIZE=1" "-sEXPORT_ES6=1" "-sEXPORT_NAME=createGen4StaticModule" "-sENVIRONMENT=worker" "-sALLOW_MEMORY_GROWTH=1" "-sFILESYSTEM=0" "-sASSERTIONS=0" "-sNO_EXIT_RUNTIME=1" "-sMALLOC=emmalloc" "-sEXPORTED_FUNCTIONS=['_gen4static_api_version','_gen4static_generate','_gen4static_search','_gen4static_result_ptr','_gen4static_result_count','_gen4static_last_error']" "-sEXPORTED_RUNTIME_METHODS=['HEAPU32']")
    set_target_properties(gen4static PROPERTIES OUTPUT_NAME "gen4static" SUFFIX ".mjs" RUNTIME_OUTPUT_DIRECTORY "${POKERNGKIT_WASM_OUTPUT_DIR}")
elseif(BUILD_TESTING)
    add_executable(gen4static_native_test bridge/gen4static_bridge.cpp tests/static4_native_test.cpp)
    target_include_directories(gen4static_native_test PRIVATE bridge "${POKEFINDER_ROOT}")
    add_test(NAME gen4static_native_parity COMMAND gen4static_native_test)
endif()
