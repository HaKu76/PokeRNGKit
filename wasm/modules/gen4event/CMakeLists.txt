if(EMSCRIPTEN)
    add_executable(gen4event bridge/gen4event_bridge.cpp)
    target_include_directories(gen4event PRIVATE bridge "${POKEFINDER_ROOT}")
    target_compile_options(gen4event PRIVATE "-O3")
    target_link_options(gen4event PRIVATE "-O3" "-sMODULARIZE=1" "-sEXPORT_ES6=1" "-sEXPORT_NAME=createGen4EventModule" "-sENVIRONMENT=worker" "-sALLOW_MEMORY_GROWTH=1" "-sFILESYSTEM=0" "-sASSERTIONS=0" "-sNO_EXIT_RUNTIME=1" "-sMALLOC=emmalloc" "-sEXPORTED_FUNCTIONS=['_gen4event_api_version','_gen4event_generate','_gen4event_search','_gen4event_result_ptr','_gen4event_result_count','_gen4event_last_error']" "-sEXPORTED_RUNTIME_METHODS=['HEAPU32']")
    set_target_properties(gen4event PROPERTIES OUTPUT_NAME "gen4event" SUFFIX ".mjs" RUNTIME_OUTPUT_DIRECTORY "${POKERNGKIT_WASM_OUTPUT_DIR}")
elseif(BUILD_TESTING)
    add_executable(gen4event_native_test bridge/gen4event_bridge.cpp tests/event4_native_test.cpp)
    target_include_directories(gen4event_native_test PRIVATE bridge "${POKEFINDER_ROOT}")
    add_test(NAME gen4event_native_parity COMMAND gen4event_native_test)
endif()
