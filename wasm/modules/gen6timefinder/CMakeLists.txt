if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  cmake_minimum_required(VERSION 3.31)
  project(PokeRNGKitGen6TimeFinderNative LANGUAGES CXX)
  set(CMAKE_CXX_STANDARD 23)
  set(CMAKE_CXX_STANDARD_REQUIRED ON)
  include(CTest)
endif()
set(SOURCES bridge/gen6timefinder_bridge.cpp)
if(EMSCRIPTEN)
  add_executable(gen6timefinder ${SOURCES})
  target_include_directories(gen6timefinder PRIVATE bridge)
  target_compile_options(gen6timefinder PRIVATE "-O3")
  target_link_options(gen6timefinder PRIVATE "-O3" "-sMODULARIZE=1" "-sEXPORT_ES6=1" "-sEXPORT_NAME=createGen6TimeFinderModule" "-sENVIRONMENT=worker" "-sFILESYSTEM=0" "-sNO_EXIT_RUNTIME=1" "-sMALLOC=emmalloc" "-sEXPORTED_FUNCTIONS=['_gen6timefinder_api_version','_gen6timefinder_initial_seed']")
  set_target_properties(gen6timefinder PROPERTIES OUTPUT_NAME "gen6timefinder" SUFFIX ".mjs" RUNTIME_OUTPUT_DIRECTORY "${POKERNGKIT_WASM_OUTPUT_DIR}")
elseif(BUILD_TESTING)
  add_executable(gen6timefinder_native_test ${SOURCES} tests/gen6timefinder_native_test.cpp)
  target_include_directories(gen6timefinder_native_test PRIVATE bridge)
  add_test(NAME gen6timefinder_initial_seed COMMAND gen6timefinder_native_test)
endif()
