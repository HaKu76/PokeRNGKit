interface:
  display_name: "HakuStyle"
  short_description: "Platform-aware Web UI style selection and implementation"
  default_prompt: "Use $web-frontend-style to inspect this Web project's platform and workflow, offer style directions when needed, then implement a coherent, accessible interface."
