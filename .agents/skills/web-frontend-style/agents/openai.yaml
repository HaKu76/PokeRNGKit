interface:
  display_name: "HakuStyle"
  short_description: "Readable rounded Web UI design from distilled references"
  default_prompt: "Use $web-frontend-style to inspect this Web project, select a task-led layout and semantic theme, then implement a readable rounded interface and run the anti-generic review."
